on: 
  pull_request:
    branches: [ main ]
  name: build CI on branch master and push hubdocker
  jobs:
    build:
      name: build CI - checkout dev - echo finish
      runs-on: ubuntu-latest
      steps:
      - uses: actions/checkout@v2
  
      - name: --- build and push to docker ---
        uses: docker/build-push-action@v1
        with:
          ref: dev
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: ${{ github.repository }}
          run: echo "finish build-push-action"

            #  push:
            #    branches: [ main ]
            #    name: build CD on branch master and push hubdocker
            #    jobs:
            #      build:
            #        name: build CD - checkout dev - create txt
            #        runs-on: ubuntu-latest
            #        steps:
            #        - uses: actions/checkout@v2
            #        - run: |
            #            date > generated.txt
            #            git config user.name github-actions
            #            git config user.email github-actions@github.com
            #            git add .
            #            git commit -m "generated"
            #            git push

#        tag_with_ref: true
#        tag_with_sha: true
#        tags: ${{ github.sha }}
    # - name: --- deploy to cluster kubernetes ---
    #   uses: steebchen/kubectl@master
    #   env:
    #     KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
    #   with:
    #     args: set image --record deployment/my-app prueba-gha=${{ github.repository }}:${{ github.sha }}
    # - name: verify deployment
    #   uses: steebchen/kubectl@master
    #   env:
    #     KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
    #     KUBECTL_VERSION: "1.15"
    #   with:
    #     args: '"rollout status deployment/my-app"'
